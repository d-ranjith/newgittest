/**
 * Converts a file size in bytes to a human-readable format with appropriate units (e.g., KB, MB, GB, TB).
 *
 * @param fileSize The size of the file in bytes to be converted.
 * @return A human-readable representation of the file size, including the appropriate unit (e.g., "2.54 MB").
 */
private String getReadableSize(long fileSize) {
    String[] units = {"bytes", "KB", "MB", "GB", "TB"};
    int unitIndex = 0;
    double size = fileSize;

    // Divide the file size by 1024 until it is smaller than 1024 or until the largest unit is reached
    while (size >= 1024 && unitIndex < units.length - 1) {
        size /= 1024;
        unitIndex++;
    }

    // Format the size with two decimal places and append the appropriate unit
    return String.format("%.2f %s", size, units[unitIndex]);
}
